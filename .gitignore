# Design tool artifacts — not needed in the repo
assets/image-slot.js
.image-slots.state.json
wireframes/
uploads/
screenshots/
*.thumbnail

# OS
.DS_Store
Thumbs.db

# Live secrets — generated at deploy time from GitHub Actions secrets,
# see config.example.php for the template
config.php
